import React from 'react';
import Menu from '../components/Menu';

const Contact = () => {
    return (
        <div className="contact">
            <Menu />
            <h1>Contact</h1>
        </div>
    );
};

export default Contact;